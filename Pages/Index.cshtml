@page
@model world_of_data.Pages.IndexModel

@{
    ViewData["Title"] = "Players";
}

<h1>Players</h1>

<div>
    <h4>Classes</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.WoWClass.className)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.WoWClass.className)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.WoWClass.classSpec)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.WoWClass.classSpec)
        </dd>
        <!-- Round 3: Loop through each course and display it -->
        @foreach (var c in Model.WoWClass.Characters)
        {
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.WoWClass.Characters)
            </dt>
            <dd class="col-sm-1">
                @Html.DisplayFor(model => c.charName)
            </dd>
            <!-- Form with button to delete each course -->
            @* <dt class="col-sm-9">
                <form method="post">
                    <input type="hidden" asp-for="CourseIdToDelete" value="@c.CourseId" />
                    <input type="submit" asp-page-handler="DeleteCourse" asp-route-id="@Model.Professor.ProfessorId" value="Delete Course" class="btn btn-danger btn-sm" />
                </form>
            </dt> *@
        }
    </dl>
</div>
@* <div>
    <a asp-page="./Edit" asp-route-id="@Model.Professor.ProfessorId">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div> *@